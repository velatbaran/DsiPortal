@model Cameras

@{
    ViewData["Title"] = "Kamera Güncelle";
}

<link rel="stylesheet" href="~/css/cameras.css" />

<div class="cameras-page">

    ```
    <!-- Sayfa Başlığı -->
    <div class="camera-page-header">

        <div class="camera-header-content">

            <div class="camera-header-icon camera-edit-header-icon">
                <i class="fa-solid fa-pen-to-square"></i>
            </div>

            <div>
                <h1>Kamera Güncelle</h1>
                <p>Mevcut kamera bilgilerini düzenleyin</p>
            </div>

        </div>

        <a asp-action="Index" class="camera-back-btn">
            <i class="fa-solid fa-arrow-left"></i>
            Kameralara Dön
        </a>

    </div>


    <!-- Form -->
    <div class="camera-form-card">

        <div class="camera-form-header">

            <div class="camera-form-header-icon">
                <i class="fa-solid fa-gear"></i>
            </div>

            <div>
                <h4>Kamera Bilgileri</h4>
                <p>
                    Kamera bağlantı ve erişim bilgilerini güncelleyin.
                </p>
            </div>

        </div>


        <form asp-action="Edit">

            <div asp-validation-summary="ModelOnly"
                 class="camera-validation-summary">
            </div>


            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CreatedDate" />
            <input type="hidden" asp-for="Created" />


            <!-- Kamera Adı -->
            <div class="camera-form-group">

                <label asp-for="Name">
                    <i class="fa-solid fa-tag"></i>
                    Kamera Adı
                </label>

                <div class="camera-input-wrapper">

                    <i class="fa-solid fa-video"></i>

                    <input asp-for="Name"
                           class="camera-input"
                           autocomplete="off" />

                </div>

                <span asp-validation-for="Name"
                      class="camera-validation">
                </span>

            </div>


            <!-- Link -->
            <div class="camera-form-group">

                <label asp-for="Link">
                    <i class="fa-solid fa-link"></i>
                    Kamera Linki
                </label>

                <div class="camera-input-wrapper">

                    <i class="fa-solid fa-globe"></i>

                    <input asp-for="Link"
                           class="camera-input"
                           autocomplete="off" />

                </div>

                <small class="camera-help">
                    Kameraya erişim sağlayan bağlantı adresi.
                </small>

                <span asp-validation-for="Link"
                      class="camera-validation">
                </span>

            </div>


            <!-- Şifre -->
            <div class="camera-form-group">

                <label asp-for="Password">
                    <i class="fa-solid fa-lock"></i>
                    Kamera Şifresi
                </label>

                <div class="camera-input-wrapper camera-password-input">

                    <i class="fa-solid fa-key"></i>

                    <input asp-for="Password"
                           class="camera-input"
                           type="password"
                           id="cameraPassword"
                           autocomplete="new-password" />

                    <button type="button"
                            class="camera-password-button"
                            id="togglePassword"
                            title="Şifreyi göster">

                        <i class="fa-solid fa-eye"></i>

                    </button>

                </div>

                <span asp-validation-for="Password"
                      class="camera-validation">
                </span>

            </div>


            <!-- Form Footer -->
            <div class="camera-form-footer">

                <a asp-action="Index"
                   class="camera-form-cancel">

                    <i class="fa-solid fa-xmark"></i>
                    İptal

                </a>


                <button type="submit"
                        class="camera-update-btn">

                    <i class="fa-solid fa-rotate"></i>
                    Değişiklikleri Kaydet

                </button>

            </div>

        </form>

    </div>
    ```

</div>

@section Scripts {

    ```
    @{
        await Html.RenderPartialAsync(
            "_ValidationScriptsPartial"
        );
    }

    <script>

        const passwordInput =
            document.getElementById('cameraPassword');

        const togglePassword =
            document.getElementById('togglePassword');


        togglePassword.addEventListener(
            'click',
            function () {

                const icon =
                    this.querySelector('i');

                if (passwordInput.type === 'password') {

                    passwordInput.type = 'text';

                    icon.classList.remove(
                        'fa-eye'
                    );

                    icon.classList.add(
                        'fa-eye-slash'
                    );

                }
                else {

                    passwordInput.type = 'password';

                    icon.classList.remove(
                        'fa-eye-slash'
                    );

                    icon.classList.add(
                        'fa-eye'
                    );

                }

            });

    </script>
```

}
